# Embedded Data Contract machine contracts

These read-only runtime copies originate from the normative
`CDEadmin_First_Party_Module_Specifications` package. They are included so
CDEadmin can validate the module manifest and screen contracts without a
developer checkout of that package. The external package remains authoritative.
